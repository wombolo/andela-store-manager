const baseApiRoute = 'http://localhost:5000/api/v1/';


const loginURL = `${baseApiRoute}auth/login`;
const signUpURL = `${baseApiRoute}auth/signup`;
const productsURL = `${baseApiRoute}products`;
const salesURL = `${baseApiRoute}sales`;
const searchProductURL = `${baseApiRoute}search/products`;
const searchSalesURL = `${baseApiRoute}search/sales`;

const profilesURL = `${baseApiRoute}profiles`;
const single_profile_URL = `${baseApiRoute}profiles/1`;


// export default {
//   loginURL,
//   signUpURL,
//   productsURL,
//   salesURL,
//   searchProductURL,
//   searchSalesURL
// }
