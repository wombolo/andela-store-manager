language: node_js
node_js:
- "stable"
cache:
  directories:
  - "node_modules"
services:
  - postgresql
before_script:
  -psql -c 'create database "andela-store-manager";' -U postgres,
  - npm run db_migrate
  - npm run db_seed
after_success:
- npm run coveralls